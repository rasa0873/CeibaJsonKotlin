<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rasaapps.raul.ceibajsonkotlin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ceiba_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ceiba_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CeibaJsonKotlin">
        <activity
            android:name=".Splash"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Publica"
            android:exported="false"
            android:label="@string/publicaciones_label" />
        <activity
            android:name=".MainActivity"
            android:exported="true">

        </activity>
    </application>

</manifest>